import { StyleSheet } from "react-native";

export const routesStyles = StyleSheet.create({
  container: {
    backgroundColor: '#25DFC9',
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  },
  text: {
    color: '#000',
    fontSize: 50
  }
});
